query GetCharacter($id: ID!) {
    character(id: $id) {
      image,
      name,
      status,
      species,
      gender,
      origin {
        id,
        name,
      },
      location {
        id,
        name
      },
      episode {
        id,
        name
      }
    }
  }
