query GetLocations($page: Int, $filter: String) {
    locations(page: $page, filter: {name: $filter}) {
        info {
            count,
            pages,
            next,
            prev
        },
        results {
            id,
            name,
            type,
            dimension,
        }
    }
}
